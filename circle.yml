machine:
  node:
    version: 0.10.22
  post:
    - ./node_modules/serve/bin/serve .:
      background: true

dependencies:
  override:
    - npm install

test:
  override:
    - phantomjs phantom.js
